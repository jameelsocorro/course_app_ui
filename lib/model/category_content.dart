class CategoryContent {
  String duration;
  String title;
  bool done;

  CategoryContent({String title, String duration, bool done}) {
    this.title = title;
    this.duration = duration;
    this.done = done;
  }
}